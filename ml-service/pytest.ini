[pytest]
# Run pytest from the ml-service dir. app/ and tests/ are importable.
pythonpath = . tests
testpaths = tests
markers =
    slow: tests that download models / need GPU / live services (opt-in).
